name: data
description: A new Flutter project.
version: 0.0.1

environment:  !include flutterVersion.yaml

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: